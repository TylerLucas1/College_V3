DELIMITER //
CREATE PROCEDURE assign_employee_to_department(IN employee_id INT, IN department_id INT)
BEGIN
    
    START TRANSACTION;
        
    LOCK TABLES department WRITE;

    UPDATE department
    SET employee_id = employee_id
    WHERE department_id = department_id;
    
    UNLOCK TABLES;
    COMMIT;
END //
DELIMITER ;